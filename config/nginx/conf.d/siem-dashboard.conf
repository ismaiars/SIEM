server {
    listen 80 default_server;
    server_name localhost;
    
    # Página principal del SIEM Dashboard
    location / {
        root /usr/share/nginx/html/siem;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
    
    # Proxy para Elasticsearch (comentado temporalmente)
    # location /elasticsearch/ {
    #     proxy_pass http://elasticsearch:9200/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     
    #     # CORS headers
    #     add_header Access-Control-Allow-Origin *;
    #     add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
    #     add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept, Authorization";
    #     
    #     if ($request_method = 'OPTIONS') {
    #         return 204;
    #     }
    # }
    
    # Proxy para Kibana (comentado hasta que kibana esté disponible)
    # location /kibana/ {
    #     proxy_pass http://kibana:5601/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     proxy_redirect off;
    # }
    
    # Proxy para Grafana (comentado hasta que grafana esté disponible)
    # location /grafana/ {
    #     proxy_pass http://grafana:3000/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     proxy_redirect off;
    # }
    
    # API de estado para verificar servicios
    location /api/status {
        access_log off;
        return 200 '{"status":"ok","timestamp":"$time_iso8601"}';
        add_header Content-Type application/json;
    }
    
    # Configuración de seguridad
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline' 'unsafe-eval'" always;
    
    # Logs
    access_log /var/log/nginx/siem-access.log;
    error_log /var/log/nginx/siem-error.log;
}